// Create Event
event_inherited();

//button_ID = "button"
//button_label = "button";
//button_action = noone;
//sprite_index = spr_btn_main_menu;
image_index = 0;
image_speed = 0;

// Allow button label to be centered
draw_set_font(fnt_setting);
label_text = string(button_label);
label_width = string_width(label_text);
label_height = string_height(label_text);


